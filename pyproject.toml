[project]
name = "pizzapy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "ipython>=9.1.0",
    "structlog>=25.2.0",
]
